# 解决聊天记录问题

## 问题分析
1. **数据库表未创建**：`chat_history` 表可能不存在
2. **前端未加载历史**：登录后没有调用历史记录接口

## 解决方案

### 第一步：创建数据库表

**方法1：使用MySQL Workbench或Navicat等图形化工具**
1. 连接到 `sizheng_db` 数据库
2. 打开 `backend/chat_history_table.sql` 文件
3. 执行SQL语句

**方法2：使用命令行（如果MySQL在PATH中）**
```bash
cd backend
mysql -u root -p sizheng_db < chat_history_table.sql
# 输入密码：123456
```

**方法3：手动执行SQL（复制粘贴到MySQL客户端）**
```sql
USE sizheng_db;

CREATE TABLE IF NOT EXISTS chat_history (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    user_id INT NOT NULL COMMENT '用户ID，关联users表',
    
    -- 用户消息相关
    user_message TEXT NOT NULL COMMENT '用户发送的消息内容',
    user_emotion_score DECIMAL(5,2) DEFAULT NULL COMMENT '用户情感评分(0-100)',
    user_ideal_belief DECIMAL(5,2) DEFAULT NULL COMMENT '理想信念强度(0-100)',
    user_logic_thinking DECIMAL(5,2) DEFAULT NULL COMMENT '逻辑思维能力(0-100)',
    user_practice_ability DECIMAL(5,2) DEFAULT NULL COMMENT '实践能力(0-100)',
    user_psychological_quality DECIMAL(5,2) DEFAULT NULL COMMENT '心理素质(0-100)',
    user_hidden_needs TEXT DEFAULT NULL COMMENT '隐性需求，逗号分隔',
    user_interest_themes TEXT DEFAULT NULL COMMENT '兴趣主题，逗号分隔',
    
    -- AI回复相关
    ai_reply TEXT NOT NULL COMMENT 'AI回复内容',
    ai_reply_score INT DEFAULT NULL COMMENT 'AI回复评分(0-100)',
    ai_reply_feedback TEXT DEFAULT NULL COMMENT 'AI回复改进意见',
    selected_model VARCHAR(50) DEFAULT NULL COMMENT '选择的模型(Qwen/DeepSeek/Kimi)',
    
    -- 模型对比信息
    qwen_score INT DEFAULT NULL COMMENT 'Qwen模型评分',
    deepseek_score INT DEFAULT NULL COMMENT 'DeepSeek模型评分',
    kimi_score INT DEFAULT NULL COMMENT 'Kimi模型评分',
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    
    -- 索引
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户与AI聊天历史记录表';
```

### 第二步：验证表已创建
执行以下SQL验证：
```sql
USE sizheng_db;
SHOW TABLES LIKE 'chat_history';
DESC chat_history;
```

### 第三步：重启服务并测试

**1. 重启后端**（已经在运行的话）
```bash
cd backend
# 按 Ctrl+C 停止，然后重新运行
python main.py
```

**2. 刷新前端页面**
- 前端代码已更新，登录时会自动加载历史记录

**3. 测试流程**
1. 登录账号
2. 发送一条消息
3. 检查数据库是否有记录：
   ```sql
   SELECT * FROM chat_history ORDER BY created_at DESC LIMIT 5;
   ```
4. 刷新页面或重新登录，查看历史记录是否显示

## 已完成的修改

### ✅ 后端修改 (main.py)
- [x] 在 `/chat` 接口中添加保存聊天记录的代码
- [x] 新增 `/chat/history` 接口用于查询历史记录
- [x] 添加类型注释修复编译错误

### ✅ 前端修改 (App.jsx)
- [x] 在 `handleAuth` 函数中添加加载历史记录的逻辑
- [x] 登录成功后自动调用 `/chat/history` 接口
- [x] 将历史记录转换为消息格式并显示

## 预期效果

### 保存功能
每次发送消息后，会自动保存以下信息：
- 用户消息内容
- 用户情感评分、理想信念、逻辑思维等5个维度
- 用户隐性需求和兴趣主题
- AI回复内容
- AI回复评分和改进意见
- 三个模型的评分对比
- 选择的最佳模型名称
- 创建时间

### 加载功能
登录后会自动加载最近30条聊天记录，包括：
- 完整的对话内容
- 每条用户消息的分析数据（情感评分、隐性需求）
- 可以点击用户消息查看详细分析

## 常见问题

**Q1：数据库报错 "Table doesn't exist"**
A：请确保执行了第一步的SQL语句创建表

**Q2：刷新后历史记录还是不显示**
A：
1. 检查浏览器控制台是否有错误
2. 检查后端日志是否有 "加载了 X 条历史记录"
3. 确认数据库中有数据：`SELECT * FROM chat_history;`

**Q3：新消息没有保存到数据库**
A：
1. 检查后端日志是否有 "聊天记录已保存，chat_id=X"
2. 检查数据库表是否正确创建
3. 重启后端服务

## 验证清单

- [ ] chat_history 表已创建
- [ ] 表结构正确（17个字段）
- [ ] 后端服务已重启
- [ ] 前端页面已刷新
- [ ] 发送消息后数据库有记录
- [ ] 重新登录后能看到历史记录
- [ ] 点击用户消息能看到分析数据
